{
  "name": "digitaalinen-tuotepassi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create digitaalinen_tuotepassi",
    "db:init": "wrangler d1 execute digitaalinen_tuotepassi --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute digitaalinen_tuotepassi --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
